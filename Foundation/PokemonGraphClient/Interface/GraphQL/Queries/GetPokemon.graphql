query GetPokemon($limit: Int, $offset: Int, $name: String) {
  pokemon: pokemon_v2_pokemon(
    limit: $limit
    offset: $offset
    where: { name: { _eq: $name } }
  ) {
    ...PokemonFragment
  }
}
